{
  "name": "bubble-server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd bubble-client && npm run build",
    "install-client": "cd bubble-client && npm install",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix bubble-client && npm run build --prefix client",
    "client": "cd bubble-client && npm start",
    "start": "node server.js"
  },
  "author": "Alex",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.11.13"
  }
}
